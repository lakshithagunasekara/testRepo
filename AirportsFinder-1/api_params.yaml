environments:
  - name: dev
    endpoints:
      production: https://localhost:9090
      sandbox:
  - name: prod
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
